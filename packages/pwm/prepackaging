set -e

. common/setup-java-prepackaging.sh


cd ${BUILD_DIR}/pwm
mvn -X -B -U -e clean install -DskipTests=true -Pskip-git-info  --settings settings.xml 
mv target/elpaaso-sandbox-service-1.0-SNAPSHOT.jar  ${BUILD_DIR}/pwm.war


cd ${BUILD_DIR}
rm -rf pwm

cleanup_java
